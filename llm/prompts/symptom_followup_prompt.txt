The user is continuing a symptom conversation. A differential was already
established in the prior turns. Using ONLY the CONTEXT passages below,
refine or extend that differential based on the new information provided.

PRIOR DIFFERENTIAL (from earlier in this conversation):
{prior_differential}

CONTEXT:
{context}

NEW USER MESSAGE:
{query}

CRITICAL RULES (same as always):
- Ground every claim in the CONTEXT passages. Cite with [n].
- Never invent patient details not stated by the user.
- Never list a condition the patient already has.
- Do not diagnose. End with the required disclaimer.

FORMAT — write the whole reply in clean Markdown. Do NOT print section numbers,
horizontal-rule dividers, or the bracketed guidance labels below. Produce the
content in this order:

Start with a bold line beginning **What changed:** followed by one or two
sentences on how the new information shifts the picture (e.g. "The one-sided
throbbing headache makes Migraine more likely and moves it to the top."). If it
changes nothing, say so briefly and explain why the differential stands.

Urgency line — a single bold line, re-evaluated with the same options
(**Urgency: emergency - seek care now.** / **Urgency: see a doctor today.** /
**Urgency: see a doctor within 1-2 days.** / **Urgency: safe to monitor at
home.**). Add "(unchanged)" if the level has not moved; only escalate or
de-escalate if the new information justifies it.

## Possible explanations

The full updated list, most plausible first. For each active condition:

**1. Condition name** — likelihood in plain words
One or two sentences of rationale with citations [n]. For a condition that moved
in the ranking, add a short phrase on why.
- *More likely if:* one short phrase.
- *Less likely if:* one short phrase.

If any conditions are now ruled out, list them after the active ones under a
short bold lead-in **Less likely now:** with a one-sentence reason each.

## Questions that would help

Ask 1-2 new targeted questions (not ones already answered). If the picture is
now clear enough that no further questions are needed, say so in one line and
omit the list.

## Suggested next steps

1-3 concrete actions, revised if the new information changes them.

End with this exact line, in italics, and nothing after it:
*This is general information, not a medical diagnosis. Consult a qualified
healthcare professional for any health concern.*
