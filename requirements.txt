# --- Core RAG ---
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
numpy>=1.26.0

# --- LLM client (Ollama via HTTP) ---
httpx>=0.27.0

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Dashboard ---
streamlit>=1.33.0

# --- Ingestion / utils ---
tqdm>=4.66.0
requests>=2.31.0
# Optional: only needed to ingest PubMedQA (downloads from Hugging Face).
# MedlinePlus and Symptom2Disease need no extra deps (XML / CSV via stdlib).
# datasets>=2.18.0

# --- ML classifier ---
xgboost>=2.0.0
scikit-learn>=1.4.0
shap>=0.45.0
datasets>=2.18.0
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Tests / eval ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
# Ragas is heavy and optional; faithfulness tests skip gracefully if absent.
# ragas>=0.1.0
