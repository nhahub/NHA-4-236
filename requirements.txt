# --- Core RAG ---
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
numpy>=1.26.0

# --- LLM client (Ollama via HTTP) ---
httpx>=0.27.0

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Dashboard ---
streamlit>=1.33.0

# --- Ingestion / utils ---
tqdm>=4.66.0
requests>=2.31.0
# Note: `datasets` (listed under ML classifier below) is also what enables the
# optional PubMedQA ingestion source. MedlinePlus and Symptom2Disease need no
# extra deps (XML / CSV via stdlib).

# --- ML classifier ---
xgboost>=2.0.0
scikit-learn>=1.4.0
shap>=0.45.0
datasets>=2.18.0          # DDXPlus download for training; also enables PubMedQA ingest
huggingface-hub>=0.23.0   # used directly by ml_model.train/evaluate + symptom_parser
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Tests / eval ---
pytest>=8.0.0
# Ragas is heavy and optional; faithfulness tests skip gracefully if absent.
# ragas>=0.1.0
