# Core runtime — everything the live app (RAG + LLM + free-text ML + scans) needs.
# For the standalone XGBoost/DDXPlus artifact and the notebooks, also install
# requirements-ml.txt.

# --- Core RAG ---
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
numpy>=1.26.0

# --- Live ML pillar: free-text symptom classifier (S-PubMedBert + LogReg) ---
scikit-learn>=1.4.0       # LogisticRegression head; also unpickles the saved model
datasets>=2.18.0          # download gretelai/symptom_to_diagnosis to train it; also
                          # enables the optional PubMedQA ingestion source
huggingface-hub>=0.23.0   # dataset/model downloads (also a sentence-transformers dep)

# --- LLM client (Ollama via HTTP) ---
httpx>=0.27.0

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9   # file uploads for the /analyze/* endpoints

# --- Dashboard ---
streamlit>=1.33.0

# --- Imaging/signal models (MRI/EEG/ECG — run live on upload) ---
# torch is pulled in transitively by sentence-transformers; torchvision is the
# MRI backbone (EfficientNet-B0). Docker installs both CPU-only first.
torchvision>=0.17.0

# --- Ingestion / utils ---
tqdm>=4.66.0
requests>=2.31.0

# --- Tests ---
pytest>=8.0.0
