"""Legacy standalone DDXPlus XGBoost artifact + free-text symptom parser.

NOT on the live path. The live symptom ML is ml_model.symptom_classifier (a
free-text classifier). These modules are kept for the portfolio notebook and
honest DDXPlus-test metrics; see ml_model/legacy/README or the project guide.
"""
